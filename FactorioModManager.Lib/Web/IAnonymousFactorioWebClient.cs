using System;

namespace FactorioModManager.Lib.Web
{
    public interface IAnonymousFactorioWebClient : IDisposable
    {
        // Task DownloadMod(string addr) // Interprets familiar urls to identify the mod, then downloads and returns it

        //
    }
}
